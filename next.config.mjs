/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "buffer.com",
          "images.unsplash.com"
        ],
      },
};

export default nextConfig;
