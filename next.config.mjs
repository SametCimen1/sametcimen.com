/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "buffer.com",
          "images.unsplash.com",
          "skillicons.dev"
        ],
      },
};

export default nextConfig;
